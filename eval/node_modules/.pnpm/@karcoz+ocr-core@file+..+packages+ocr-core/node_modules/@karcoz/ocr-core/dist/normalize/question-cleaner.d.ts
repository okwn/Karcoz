import { NormalizeResult } from '../types.js';
/**
 * Full normalization pipeline: whitespace → Turkish → math symbols → punctuation.
 */
export declare function normalizeText(text: string, languageHint?: 'en' | 'tr'): NormalizeResult;
//# sourceMappingURL=question-cleaner.d.ts.map