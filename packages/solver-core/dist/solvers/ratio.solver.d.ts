import type { SolverQuestion, SolverResult } from '../types.js';
/**
 * Ratio solver — handles "a:b = c:d", "oran", "doğru orantı", "ters orantı"
 */
export declare function solveRatio(question: SolverQuestion): SolverResult;
