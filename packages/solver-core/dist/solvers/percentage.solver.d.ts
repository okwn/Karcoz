import type { SolverQuestion, SolverResult } from '../types.js';
/**
 * Percentage solver — handles "X percent of Y is what?", "what is X% of Y?"
 */
export declare function solvePercentage(question: SolverQuestion): SolverResult;
